# 📊 Prometheus + Grafana

```text
Linux -> node_exporter -> Prometheus -> Grafana -> Alerting
```

Monitor CPU, memory, load, filesystem, disk I/O, network and service health. Every alert should have severity, threshold, owner and runbook.
